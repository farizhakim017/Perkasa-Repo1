import 'package:flutter/material.dart';

import '../components/searchbar.dart';

class SearchUpPage extends StatelessWidget {
  const SearchUpPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: SearchScreen(context));
  }
}
